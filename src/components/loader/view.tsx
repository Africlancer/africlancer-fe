import React from 'react'
import { ApLoader } from './main'

export const ApViewLoader = () => {
  return (
    <div className='absolute top-0 w-full h-full bg-white/90 flex items-center justify-center'>
        <ApLoader/> 
    </div>
  )
}